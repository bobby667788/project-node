
  <% include _layout/header%>

  <% if(user) {%>
  <% include _layout/form%>
  <%} else{%>
  <center>
  <header class="jumbotron hero-spacer" style="margin:2px auto;">
                    <h1>A Warm Welcome!</h1>
                    <br>
                    <p>Please login before posting any blogs..</p>
                    <br>
                    <p><a class="btn btn-primary btn-lg" href="/signup">Sign Up Now!</a>
                    </p>
    </header></center>
     <%}%>
  
  <% include _layout/footer%>
 

	<script>
        var optionsVal = document.getElementById('sel1');
        var input = document.getElementById('cat1');
               optionsVal.onclick = function(){
        setVal(this);
        };
        $("#authput").keyup(function(){
        update();
        });

        function update() {
        $("#authfill").val($('#authput').val());
        }
        
        function setVal(selectedVal){
         input.value = selectedVal.value;
        }
    
    
    
    </script>

